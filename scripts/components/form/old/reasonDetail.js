export default function ReasonDetail(){

    const labels = `
    <label for="reason-detail"></label>
    <select id="reason-detail" name="reason-detail" >
        <option value="null">Seleccione una opcion</option>
    </select>
    `
    //las opciones del select se hacen con el otro script 
    return(labels)
}